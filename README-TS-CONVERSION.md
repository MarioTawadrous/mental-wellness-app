This project was converted to TypeScript by the assistant.
- Entry point: App.tsx
- tsconfig.json is included.
- Declarations added for images and svgs.
- You may need to run `npm install` and then `expo start`.
- If you want I can further refine types for props across components.